package maps;

public enum Direction {
	// The possible movements from one point to the next
	// None is used for no movement so paths don't have to have the same length.
	UP, DOWN, LEFT, RIGHT, NONE;
}
